# Context7 docs helper

Use Context7 to fetch up-to-date documentation when researching libraries. Keep it concise and cite what you fetched.

- "Fetch docs for <library> /topic:<topic> #context7"
- Prefer exact package names; resolve first if ambiguous.
- Summarize findings and link the canonical source.
- Avoid duplication—quote minimal excerpts only when necessary.
