# Story 2.0: Implement CI/CD Confidence Net

## Status

Approved

## Story

**As a** developer,
**I want** a fast, reliable, and automated CI/CD pipeline with robust testing guardrails,
**so that** I can ship new features with confidence and without introducing regressions.

## Acceptance Criteria

1. The `.github/workflows/ci.yml` file is updated to run three parallel jobs: `lint-and-format`, `unit-tests`, and `integration-tests`.
2. The CI workflow includes caching for Deno dependencies and Supabase Docker images to ensure fast execution times on subsequent runs.
3. The `integration-tests` job successfully starts a local Supabase instance and runs only the `*.integration.test.ts` files.
4. The `unit-tests` job runs only the `*.unit.test.ts` files and does not require a Supabase instance.
5. A new `TESTING.md` document is created in the `docs` directory, explaining the "Confidence Net" strategy and how to write integration tests.
6. A new `tests/_test_utils.ts` file is created to contain standardized helper functions for setting up test data and mock clients.
7. The `main` branch on GitHub is protected, requiring all CI checks to pass before a pull request can be merged (to be verified manually).

## Tasks / Subtasks

- [ ] **Task 1:** Create the `docs/TESTING.md` document. (AC: #5)
  - [ ] Subtask 1.1: Create the file `docs/TESTING.md`.
  - [ ] Subtask 1.2: Write the "Confidence Net" philosophy section, explaining the three layers (Local Hooks, CI, Manual Sanity Check).
  - [ ] Subtask 1.3: Write the "Unit Tests (`*.unit.test.ts`)" section, detailing their purpose (isolated, fast, no external dependencies) and providing a code example.
  - [ ] Subtask 1.4: Write the "Integration Tests (`*.integration.test.ts`)" section, explaining their purpose (testing component interactions with a real local DB) and providing a code example.
  - [ ] Subtask 1.5: Add a "CI Configuration" section briefly explaining how the `lint-and-format`, `unit-tests`, and `integration-tests` jobs run in parallel on GitHub Actions.

- [ ] **Task 2:** Create the `tests/_test_utils.ts` file for shared testing helpers. (AC: #6)
  - [ ] Subtask 2.1: Create the file `tests/_test_utils.ts`.
  - [ ] Subtask 2.2: Implement and export a helper function `createMockSupabaseClient()` for unit tests that need to mock the database interface.
  - [ ] Subtask 2.3: Implement and export helper functions for managing integration test data, such as `seedTestData()` and `cleanupTestData()`.

- [ ] **Task 3:** Implement the new CI workflow file. (AC: #1, #2, #3, #4)
  - [ ] Subtask 3.1: Create the file `.github/workflows/ci.yml` and populate it with the exact YAML content provided in the `Dev Notes`.

- [ ] **Task 4:** Refactor existing test files to the new naming convention. (AC: #3, #4)
  - [ ] Subtask 4.1: Rename `tests/book_addition_test.ts` to `tests/book_addition.integration.test.ts`.
  - [ ] Subtask 4.2: Rename `tests/csv_backfill_test.ts` to `tests/csv_backfill.integration.test.ts`.
  - [ ] Subtask 4.3: Rename `tests/enrich_metadata_test.ts` to `tests/enrich_metadata.integration.test.ts`.
  - [ ] Subtask 4.4: Rename `tests/rss_ingestion_test.ts` to `tests/rss_ingestion_legacy.integration.test.ts` to distinguish it from the newer, more specific RSS tests.
  - [ ] Subtask 4.5: Rename `tests/seed_lookup_data_test.ts` to `tests/seed_lookup_data.integration.test.ts`.
  - [ ] Subtask 4.6: Rename `tests/webhook_test.ts` to `tests/webhook.integration.test.ts`.
  - [ ] Subtask 4.7: Verify that all tests still run correctly after renaming using the new CI job logic (`deno test -A --ignore=**/*.unit.test.ts` for integration tests).

- [ ] **Task 5:** Verify the complete CI pipeline and branch protection. (AC: #7)
  - [ ] Subtask 5.1: Ensure all previous tasks are complete and all changes are committed to the feature branch.
  - [ ] Subtask 5.2: Create a new Pull Request on GitHub targeting the `main` branch.
  - [ ] Subtask 5.3: Observe the "Checks" tab on the Pull Request and confirm that the `lint-and-format`, `unit-tests`, and `integration-tests` jobs are triggered and run in parallel.
  - [ ] Subtask 5.4: Confirm that all jobs pass successfully.
  - [ ] Subtask 5.5: (Manual Check) Confirm that the GitHub branch protection rule for `main` prevents merging the PR until all checks are complete and successful.

## Dev Notes

### New CI/CD Workflow

The primary deliverable for this story is the updated `.github/workflows/ci.yml` file. The developer agent must replace the existing content with the following verified configuration:

```yaml
name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  # Job 1: Fast checks for code style and formatting
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      # Cache Deno dependencies based on the lock file
      - name: Cache Deno dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ hashFiles('**/deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-
      - name: Run Deno lint
        run: deno lint
      - name: Run Deno format check
        run: deno fmt --check --ignore=docs/,README.md

  # Job 2: Fast checks for pure business logic
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      # Use the same dependency cache as the lint job
      - name: Cache Deno dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ hashFiles('**/deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-
      # Run only unit tests, which are fast and have no external dependencies
      - name: Run Unit Tests
        run: deno test -A --ignore=**/*.integration.test.ts

  # Job 3: Slower, more comprehensive tests that require a full database
  integration-tests:
    runs-on: ubuntu-latest # Using a standard runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      # Use the same dependency cache
      - name: Cache Deno dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ hashFiles('**/deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-
      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest
      # Cache the Supabase Docker images to speed up 'supabase start'
      - name: Cache Supabase Docker images
        uses: actions/cache@v4
        with:
          path: /tmp/.docker-images-supabase.tar
          key: ${{ runner.os }}-supabase-docker-images
      # Load cached Docker images if they exist
      - name: Load Docker images
        run: |
          if [ -f /tmp/.docker-images-supabase.tar ]; then
            docker load --input /tmp/.docker-images-supabase.tar
          fi
      - name: Start Supabase local instance
        run: supabase start
      # Save Docker images to the cache path for the next run
      - name: Save Docker images
        run: |
          docker images --format '{{.Repository}}:{{.Tag}}' | grep 'supabase/' | xargs docker save --output /tmp/.docker-images-supabase.tar
      # Run only integration tests
      - name: Run Integration Tests
        run: deno test -A --ignore=**/*.unit.test.ts
        env:
          SUPABASE_URL: http://127.0.0.1:54321
          SUPABASE_SERVICE_ROLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU
          TELEGRAM_BOT_TOKEN: test-bot-token-for-ci
          TELEGRAM_WEBHOOK_SECRET: test-webhook-secret-for-ci
          GOOGLE_GEMINI_API_KEY: test-gemini-key-for-ci
```

### Testing

- **Testing Strategy:** The new `TESTING.md` document will become the source of truth. For this story, the primary validation is a successful run of the new CI pipeline on a test Pull Request.
- **Test File Naming:** Existing tests must be refactored to use the `*.unit.test.ts` or `*.integration.test.ts` suffix to be correctly picked up by the new parallel jobs.

## Change Log

| Date | Version | Description |
| ---- | ------- | ----------- |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
